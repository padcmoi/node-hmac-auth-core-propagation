node_modules
dist
coverage
.tsbuildinfo
.tsup
.nyc_output

# Framework build artefacts of the POC apps. Generated in the container, never committed.
.next
.nuxt
.output
next-env.d.ts

# Scratch space (specs, plans, drafts, antipatterns), never committed
.trash/

# Local tgz packed from sibling lib for dev wiring
*.tgz

# Lockfile-less by design for a library; consumers own their lockfile
package-lock.json
pnpm-lock.yaml
yarn.lock

# Logs
logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea
